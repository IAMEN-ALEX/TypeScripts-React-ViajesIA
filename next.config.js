/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        // appDir: true, // No longer needed in recent versions but good to be safe if older
    },
};

module.exports = nextConfig;
